{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Next.js dev (webpack — fixes Turbopack ENOENT)",
      "type": "shell",
      "command": "rm -rf .next && node scripts/dev.js",
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "group": {
        "kind": "build",
        "isDefault": true
      },
      "presentation": {
        "reveal": "always",
        "panel": "new"
      }
    }
  ]
}
